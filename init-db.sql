-- Initialize P1 Energy Monitor database
-- This file is automatically run by PostgreSQL on container startup

-- Create p1db database if it doesn't exist
-- (Note: POSTGRES_DB already creates this, but this is a safety measure)
SELECT 1;
